#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int solution(int n) {
    for (int i = 3; i < n; i++)
    {
        if (n % i == 1)
            return answer;
    }
}

int main()
{
    vector<int> arr = {3,2,6};
    auto s = solution(arr, 10);
    return 0;
}